import cron from "node-cron";const base=process.env.NEXUS_URL||"http://localhost:3000";async function tick(){const r=await fetch(base+"/api/schedules");if(!r.ok)return;for(const s of (await r.json()).filter(x=>x.enabled&&new Date(x.nextRunAt||0)<=new Date())){const agents=await fetch(base+"/api/agents").then(r=>r.json());const a=agents.find(x=>x.id===s.agentId);if(a)await fetch(base+`/api/agents/${a.id}/run`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({task:s.prompt})})}}cron.schedule("*/1 * * * *",tick);console.log("NEXUS scheduler worker",base);tick();
