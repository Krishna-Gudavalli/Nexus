import{NextResponse}from"next/server";import{getModels,saveModels}from"@/lib/platform-store";export async function GET(){return NextResponse.json(await getModels())}export async function PATCH(req:Request){const m=await getModels();const p=await req.json();const i=m.findIndex(x=>x.id===p.id);if(i<0)return NextResponse.json({error:"Model not found"},{status:404});m[i]={...m[i],...p};await saveModels(m);return NextResponse.json(m[i])}
