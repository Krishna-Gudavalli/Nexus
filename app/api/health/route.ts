import {NextResponse} from "next/server";import{dbHealth}from"@/lib/db";export async function GET(){return NextResponse.json({ok:true,service:"NEXUS",status:"healthy",timestamp:new Date().toISOString(),aiConfigured:Boolean(process.env.AI_API_KEY),githubConfigured:Boolean(process.env.GITHUB_TOKEN||process.env.GITHUB_CLIENT_ID),webSearchConfigured:Boolean(process.env.TAVILY_API_KEY),database:await dbHealth()})}
