import{NextResponse}from"next/server";import{getPatches,savePatches}from"@/lib/platform-store";export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const{action}=await req.json();const patches=await getPatches();const i=patches.findIndex(p=>p.id===id);if(i<0)return NextResponse.json({error:"Patch not found"},{status:404});if(action!=="approve"&&action!=="reject")return NextResponse.json({error:"Use approve or reject"},{status:400});patches[i]={...patches[i],status:action==="approve"?"approved":"rejected"};await savePatches(patches);return NextResponse.json(patches[i])}
