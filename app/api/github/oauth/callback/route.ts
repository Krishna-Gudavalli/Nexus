import{NextResponse}from"next/server";export async function GET(req:Request){const code=new URL(req.url).searchParams.get("code");if(!code)return NextResponse.json({error:"Missing code"},{status:400});if(!process.env.GITHUB_CLIENT_ID||!process.env.GITHUB_CLIENT_SECRET)return NextResponse.json({error:"GitHub OAuth is not configured"},{status:503});const r=await fetch("https://github.com/login/oauth/access_token",{method:"POST",headers:{Accept:"application/json","Content-Type":"application/json"},body:JSON.stringify({client_id:process.env.GITHUB_CLIENT_ID,client_secret:process.env.GITHUB_CLIENT_SECRET,code})});const d=await r.json();if(!d.access_token)return NextResponse.json({error:"OAuth exchange failed"},{status:401});return NextResponse.json({connected:true,tokenReceived:true,note:"Persist OAuth credentials only in a secure session/secret store."})}
