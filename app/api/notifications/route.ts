import{NextResponse}from"next/server";import{z}from"zod";const schema=z.object({webhook:z.string().url(),message:z.string().min(1).max(4000)});export async function POST(req:Request){const b=schema.parse(await req.json());const r=await fetch(b.webhook,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({content:b.message,text:b.message}),cache:"no-store"});return NextResponse.json({ok:r.ok,status:r.status})}
