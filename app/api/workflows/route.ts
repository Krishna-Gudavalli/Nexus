import{NextResponse}from"next/server";import{z}from"zod";import{getWorkflows,saveWorkflows}from"@/lib/platform-store";
const node=z.object({id:z.string(),type:z.enum(["trigger","agent","tool","condition","approval","output"]),label:z.string(),config:z.record(z.string(),z.unknown()).optional()});const schema=z.object({name:z.string().min(2),description:z.string().default(""),nodes:z.array(node).min(1),enabled:z.boolean().default(false)});
export async function GET(){return NextResponse.json(await getWorkflows())}export async function POST(req:Request){const b=schema.parse(await req.json());const all=await getWorkflows();const w={id:crypto.randomUUID(),...b,createdAt:new Date().toISOString()};all.unshift(w);await saveWorkflows(all);return NextResponse.json(w,{status:201})}
