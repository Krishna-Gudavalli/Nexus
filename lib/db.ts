import { Pool } from "pg"; let pool:Pool|null=null; export function getDb(){if(!process.env.DATABASE_URL)return null;if(!pool)pool=new Pool({connectionString:process.env.DATABASE_URL,max:5,ssl:process.env.DATABASE_SSL==="true"?{rejectUnauthorized:false}:undefined});return pool} export async function dbHealth(){const db=getDb();if(!db)return{configured:false};try{await db.query("select 1");return{configured:true,healthy:true}}catch{return{configured:true,healthy:false}}}
